/**
 * This package contains numerous packages and classes that are used
 * during the compilation and execution of a program.
 *
 * The classes in this package are the BaseNode (contains all of the code to be executed).
 * EmptyNode (represents an empty line in the program).
 * EvaluateNode (evaluates a given statement during execution).
 * PrintNode (prints all print statements in the program).
 */

package parser.nodes;