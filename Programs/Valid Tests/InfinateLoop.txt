***This program tests functions such as eq, gt and lt in a mostly infinite loop. It is not meant to solve the maze***



Stack toProcess;
Node parent = Maze.getStart();
toProcess.add(parent);

***Test the eq function***
while (!eq(toProcess.getSize(), 20)) {
    print("I'm a mostly infinite loop");
    toProcess.add(parent);
}

print("Loop done");